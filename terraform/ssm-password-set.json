{
    "commands":  [
                     "Import-Module ActiveDirectory",
                     "$password = ConvertTo-SecureString \"Welcome2025!\" -AsPlainText -Force",
                     "Set-ADAccountPassword -Identity jane.doe -NewPassword $password -Reset -Server innovatech.local",
                     "Enable-ADAccount -Identity jane.doe -Server innovatech.local",
                     "Get-ADUser -Identity jane.doe -Server innovatech.local -Properties PasswordLastSet,Enabled | Format-List Name,Enabled,PasswordLastSet"
                 ]
}